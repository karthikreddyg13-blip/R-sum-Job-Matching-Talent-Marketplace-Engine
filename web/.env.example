# Optional: override the backend base URL (default uses the /api proxy → localhost:4000).
# Set to your deployed API host in production, e.g.:
# NEXT_PUBLIC_API_BASE=https://talentmatch-api.onrender.com
NEXT_PUBLIC_API_BASE=
