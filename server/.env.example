# ---- Server mode -------------------------------------------------------
PORT=4000
JWT_SECRET=talentmatch-dev-secret-change-me
DB_MODE=json

# ---- Supabase (optional; when set, auth + DB write-through switch on) ---
# Project Settings → API in the Supabase dashboard
SUPABASE_URL=
SUPABASE_ANON_KEY=
# service_role key: server-side only, bypasses RLS (engine writes, seeding)
SUPABASE_SERVICE_ROLE_KEY=
